import { OrderStatusRequest } from './order-status-request';

describe('OrderStatusRequest', () => {
  it('should create an instance', () => {
    expect(new OrderStatusRequest()).toBeTruthy();
  });
});
