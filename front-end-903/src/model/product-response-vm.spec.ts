import { ProductResponseVm } from './product-response-vm';

describe('ProductResponseVm', () => {
  it('should create an instance', () => {
    expect(new ProductResponseVm()).toBeTruthy();
  });
});
