import { RequestOrderResponse } from './request-order-response';

describe('RequestOrderResponse', () => {
  it('should create an instance', () => {
    expect(new RequestOrderResponse()).toBeTruthy();
  });
});
