import { ProductDetailsResponse } from './product-details-response';

describe('ProductDetailsResponse', () => {
  it('should create an instance', () => {
    expect(new ProductDetailsResponse()).toBeTruthy();
  });
});
